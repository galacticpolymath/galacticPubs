#' Copy directory selectively
#'
#' Copy a folder, omitting certain items. Use case: using Gdrive for desktop virtualization, copy a folder, but ignore Gdrive files
#'
#' @param source_path source path on local file system
#' @param dest_path full destination path
#' @param ignore_gdrive logical; default=TRUE, ignore gdrive files (.gdoc,.gsheet,.gslide,.gform,.gdraw)
#' @export

copy_dir_selectively <- \(source_path,
                          dest_path,
                          ignore_gdrive=TRUE){



}

